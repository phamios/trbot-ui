export type TWallet = {
	address: string;
};

export type TWalletBalance = {
	balanceInHex: string;
	balanceInWei: string;
	balance: string;
};

export type TWalletApprovalStatus = {
	approved: boolean;
};
